# Display Studio Proxy Configuration

# Port for the proxy server (default: 3001)
PORT=3001

# Matrix switcher device IP address (default: 192.168.1.122)
MATRIX_DEVICE_IP=192.168.1.122

# Matrix device Basic Auth credentials in format "username:password" (default: admin:admin)
MATRIX_DEVICE_AUTH=admin:admin
